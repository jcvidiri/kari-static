## Kari-static

> home  ->  index
>  cur   ->  curriculum
>  soc   ->  social
>  inf   ->  information
>  con   ->  contact  

Use `gulp build` to create your dist in /build and then `gulp inject-own` to inject all dependencies.
